import { ScheduledTicketWork } from './scheduledTicketWork';

export class ScheduledCorePreviouslyInterruptedTicketWork extends ScheduledTicketWork {
  // Represents follow-up work for a work iteration that was interrupted and context
  // had to be re-acquired.
}
